{% set heading_id = attributes.id ~ '-menu'|clean_id %}
{% set menu_id = heading_id ~ '--links' %}
{% include '@emulsify/02-molecules/navigation/primary-nav/primary-nav.twig' with {
  attributes: attributes,
  label: configuration.label ?: 'Main navigation'|t,
  toggle_label: 'Menu'|t,
  content: content,
  menu_id: menu_id,
} %}
