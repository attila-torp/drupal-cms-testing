{
  "name": "labl-theme",
  "version": "1.1.0",
  "private": true,
  "description": "Drupal theme powered by the Emulsify design system",
  "scripts": {
    "design-system": "npm run develop --prefix design-system",
    "design-system:build": "npm run build --prefix design-system",
    "design-system:lint": "npm run lint --prefix design-system"
  }
}
