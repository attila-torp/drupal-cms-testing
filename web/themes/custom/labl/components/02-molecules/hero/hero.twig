<section class="c-hero">
  <div class="c-hero__content">
    <h1 class="c-hero__title">{{ title }}</h1>
    {% if body %}
      <p class="c-hero__body">{{ body }}</p>
    {% endif %}
    {% if cta %}
      <div class="c-hero__cta">{{ cta }}</div>
    {% endif %}
  </div>
</section>
